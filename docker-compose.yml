version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: votecast-app
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - APP_URL=http://localhost:8000
      # Database (using your Supabase)
      - DB_CONNECTION=pgsql
      - DB_HOST=aws-1-ap-northeast-2.pooler.supabase.com
      - DB_PORT=5432
      - DB_DATABASE=postgres
      - DB_USERNAME=postgres.mvpilbxfdofrubjypyii
      - DB_PASSWORD=dSaYa9rqcQ82dC1L
      - DB_SSLMODE=require
      - DB_SCHEMA=VoteCast
      # Cloudinary
      - CLOUDINARY_CLOUD_NAME=dmivebhla
      - CLOUDINARY_API_KEY=726265782827729
      - CLOUDINARY_API_SECRET=IcEW-QzwklAAxrOZLSJ-LwDR284
    volumes:
      - ./storage:/var/www/storage
      - ./public/uploads:/var/www/public/uploads
      - ./logs:/var/www/storage/logs
    restart: unless-stopped
    networks:
      - votecast-network

networks:
  votecast-network:
    driver: bridge
